<view class="weui-toptips {{className}} {{extClass}} {{show ? 'weui-toptips_show' :  ''}}" style="{{show?'top:'+top+'px':''}};">
    <block wx:if="{{msg}}">{{msg}}</block>
    <block wx:else>
        <slot></slot>
    </block>
</view>